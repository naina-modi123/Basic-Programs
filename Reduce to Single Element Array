public static boolean canReduce(int N, int[] arr) {
    //write your code here
    Arrays.sort(arr);
    for(int i = 1;i<N;i++){
        if(arr[i] - arr[i-1] > 1) return false;
    }
    return true;
}
